# trigger:
#  - master

pool:
  vmImage: ubuntu-latest
variables:
- group: New variable group 27-Dec

steps: 

- script: |
    python -m pip install --upgrade pip
    pip install semgrep
    semgrep ci
  env: 
    SEMGREP_PR_ID: $(System.PullRequest.PullRequestNumber)
